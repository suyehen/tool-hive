# ToolHive 一期验收报告 v2.0

> 验收日期：2026-09-03  
> 验收范围：当前仓库 `docs` 目录内的一期需求  
> 验收方式：独立核查后端、前端、SQL、配置、部署脚本与自动化验证结果

## 一、验收范围声明

### 纳入验收的文档

1. `docs/readme.md`（文档对齐规则）
2. `docs/ToolHive总体功能与架构说明.md`
3. `docs/一期功能/一期目标.md`
4. `docs/一期功能/一期上半已实现功能.md`
5. `docs/一期功能/一期下半待实现功能.md`
6. `docs/一期功能/一期下半开发计划.md`
7. `docs/一期功能/一期下半设计冻结.md`
8. `docs/一期功能/一期开发完成情况.md`
9. `docs/一期功能/验收报告/一期验收报告.md`（仅作范围和已确认边界交叉核对）
10. `docs/一期功能/验收报告/20260902-代码功能与架构核验报告.md`（仅作范围和已确认边界交叉核对）

### 明确排除项

- `docs/old`：归档目录，完全不参与本次验收，不引用其中内容，也不以其中内容判定偏差。
- 二期需求：`docs/二期功能/二期目标.md`，以及《一期目标》§5、《一期下半待实现功能》§13、《一期下半开发计划》§8 中的本期明确不做内容，统一标记为【本期不涉及】。

## 二、一期需求逐条核对表

| 需求编号 | 需求描述 | 状态 | 证据（文件+标识） | 差异说明 |
|---|---|---|---|---|
| P1-01 | 统一应用、CPython 3.11.6 生产约束、管理与运行模块同一部署单元 | ✅已实现 | `tool-hive/pyproject.toml:11`：`requires-python`；`tool-hive/src/toolhive/main.py:21`：`lifespan`、双子应用挂载 | 无 |
| P1-02 | 首个超管、账号生命周期、临时密码、重置、强制下线和离职 | ✅已实现 | `tool-hive/src/toolhive/services/account_service.py:149`：`create_account`；`:348`：`reset_password`；`:472`：`offboard_account`；`tool-hive/src/toolhive/cli.py:210`：`_init_admin` | 无 |
| P1-03 | 账密+图形验证码、密码策略、失败限制、互斥会话、CSRF | ✅已实现 | `tool-hive/src/toolhive/api/admin/auth/router.py:67`：验证码；`tool-hive/src/toolhive/services/auth_service.py:50`：`login_password`；`tool-hive/src/toolhive/api/admin/middleware.py:1`：会话/CSRF 中间件 | 无 |
| P1-04 | 管理角色、操作项、多对多、默认拒绝和超管保护 | ✅已实现 | `tool-hive/src/toolhive/api/admin/deps.py:51`：`require_operation`；`tool-hive/src/toolhive/services/role_service.py:410`：`get_effective_operations`；`:547`：超管角色同步 | 无 |
| P1-05 | 调用系统、有效公钥、来源 IP、运行策略、生命周期和紧急禁用 | ✅已实现 | `tool-hive/src/toolhive/services/caller_system_service.py:225`：启用前置校验；`:872`：`verify_ip`；`:902`：`_check_enable_conditions` | 无 |
| P1-06 | 管理前端及按钮级权限控制 | ✅已实现 | `frontend/src/App.tsx:12`：管理/Catalog 页面路由；`frontend/src/layouts/AdminLayout.tsx:35`：菜单操作项控制 | 无 |
| P1-07 | Catalog 的 Provider、能力包、工具、版本、绑定及数据库模型 | ✅已实现 | `tool-hive/sql/init.sql:577`：`catalog_provider`；`:605`：能力包；`:629`：工具；`:669`：版本；`:702`：执行绑定 | 无 |
| P1-08 | 工具版本送审、通过、驳回、发布、默认版本、停用、撤回和归档 | ✅已实现 | `tool-hive/src/toolhive/services/catalog_version_service.py:260`：`submit_review`；`:289`：`approve`；`:331`：`publish`；`:361`：`set_default` | 无 |
| P1-09 | 运行入口、可信代理、签名认证、时间窗、Nonce 防重放和统一错误体 | ✅已实现 | `tool-hive/src/toolhive/api/ingress.py:35`：`IngressMiddleware`；`tool-hive/src/toolhive/runtime/authentication/service.py:69`：`build_canonical`；`:100`：`authenticate` | 无 |
| P1-10 | 能力包与调用系统的授权关联必须实际影响运行授权 | ⚠️部分实现 | `tool-hive/src/toolhive/services/catalog_capability_service.py:281`：`replace_pack_systems`；`tool-hive/sql/init.sql:813`：`catalog_capability_pack_system` | 运行侧 `runtime/tool_control/` 未查询该授权关系，仅依据 `CallerToolScope`；能力包页面维护的调用系统授权不会直接改变 Discover、Resolve、Execute 结果。 |
| P1-11 | 工具、能力包、命名空间三类运行范围实时控制 | ⚠️部分实现 | `tool-hive/src/toolhive/core/enums.py:69`：`ToolScopeType` 仅有 `capability`、`tool`；`tool-hive/src/toolhive/runtime/tool_control/service.py:291`：仅检查工具/能力包 | 文档要求命名空间范围；当前接口、枚举、表模型及运行授权均未支持。 |
| P1-12 | ToolContext、流量控制、确认、Resolve/Discover/Execute 和参数校验 | ✅已实现 | `tool-hive/src/toolhive/api/runtime/v1/tools.py:75`、`:125`、`:169`；`tool-hive/src/toolhive/runtime/control/traffic.py:39`；`tool-hive/src/toolhive/runtime/confirmations/service.py:44` | 租户/业务身份规则化过滤已标记为后续范围；一期声明制上下文契约已实现。 |
| P1-13 | Provider 固定映射、HTTPS/TLS、SSRF、资源限制、重试、熔断和健康检查 | ✅已实现 | `tool-hive/src/toolhive/runtime/execution/outbound.py:185`：固定出站构造；`http_executor.py:33`：`HttpExecutor`；`:146`：响应限制；`api/admin/catalog/providers.py:134`：启用前健康检查 | 规则化响应脱敏/转换已标记为后续范围，不计本期差异。 |
| P1-14 | PostgreSQL 事实源、Chroma/Embedding 检索、关键词降级、Outbox 和重建 | ✅已实现 | `tool-hive/src/toolhive/runtime/retrieval/service.py:54`：`RetrievalService`；`tool-hive/src/toolhive/services/outbox/worker.py:31`：`OutboxWorker`；`tool-hive/src/toolhive/cli.py:17`：`rebuild-chroma` | 无 |
| P1-15 | 基础 Trace、管理审计、关联、脱敏边界 | ✅已实现 | `tool-hive/src/toolhive/runtime/tracing/service.py:41`：`TraceService`；`tool-hive/src/toolhive/services/audit_service.py:83`：`sanitize_summary`；`tool-hive/sql/init.sql:844`：`runtime_trace_log` | 冻结设计 §12 已明确一期 Trace 的明文例外，未按缺陷处理。 |
| P1-16 | Nginx、静态前端、生产配置校验、初始化/启动/验收脚本 | ✅已实现 | `deploy/nginx/toolhive.conf:20`、`:92`：管理/运行入口；`tool-hive/src/toolhive/config.py:414`：生产校验；`tool-hive/scripts/verify.sh:49`：签名 E2E 验证 | 无 |
| P2-01 | 已标注二期及本期明确不做的内容 | ⏭️二期不涉及 | `docs/二期功能/二期目标.md:1`；`docs/一期功能/一期目标.md:147`；`一期下半待实现功能.md:180` | 【本期不涉及】 |

## 三、本期偏差清单

1. **高**：P1-10 能力包—调用系统授权关联未接入运行时决策。管理端新增或删除该授权关系，不会直接授予或撤销调用系统对能力包内工具的访问。
2. **中**：P1-11 未实现命名空间级工具范围。当前只能逐工具或逐能力包授权，不能按冻结设计配置并执行命名空间范围。

## 四、一期验收结论

- **结论**：有条件通过。
- **前置修复项**：
  1. 将 `catalog_capability_pack_system` 纳入运行侧调用控制，确保能力包—调用系统授权变更实时影响 Discover、Resolve、Execute。
  2. 补齐命名空间范围的配置、引用校验和 Discover、Resolve、Execute 实时判断。
- **一句话总结**：一期文档需求共 16 条，已实现 14 条，遗留 2 条。

## 五、验证记录

- 后端执行 `python -m pytest`：422 项通过。
- 后端执行 `python -m ruff check src tests`：通过。
- 前端执行 `npm run build`：通过。
- 本次核验机解释器为 Python 3.12.4；文档规定的 CPython 3.11.6 生产运行基线已由 `pyproject.toml` 声明，但未在本机以 3.11.6 进行实测。
