# ToolHive 二期 · MCP 接入开发计划

> 文档版本：v0.1
> 更新日期：2026-09-07
> 文档目的：将 MCP 接入功能拆解为可执行的功能清单与实施顺序，作为任务拆分、开发执行和阶段验收的依据。
> 范围说明：正式交付范围与边界以《需求》《设计冻结》为准；D1（MCP 风险确认流程）延后，不阻塞本计划。

## 1. 阶段目标

MCP 接入形成最小、可控的 MCP 工具调用闭环：

```text
管理员配置 MCP Server 与客户端
  → 客户端 Bearer Token 认证
  → MCP 客户端授权（A2）与工具暴露判定（tools/list）
  → tools/call 复用 Catalog execute 决策链
  → Provider 执行返回标准化结果
  → Trace 落库，管理端可查 MCP 调用记录
```

HTTP 与 MCP 两条链路隔离，共享 Catalog 工具资产与治理服务；本批只做 MCP Server（方向 A），同步执行，风险确认流程延后（D1）。

## 2. 实施前置：已冻结设计

实现依据《需求》与《设计冻结》。关键前置如下：

| # | 主题 | 冻结/状态 |
|---|------|-----------|
| 1 | SDK 与部署 | 官方 SDK v2（mcp>=2,<3），`streamable_http_app()` 挂载进现有 FastAPI；宿主 lifespan 进入 `session_manager.run()` |
| 2 | 端点与能力 | 独立 `/mcp` 路径（可配置），仅声明 tools，协议版本跟 SDK 协商 |
| 3 | 认证 | Bearer Token 一客户端一 Token；只存哈希；SDK TokenVerifier 校验；来源限制默认内网 |
| 4 | 授权 | 显式授权 + 默认拒绝；A2（命名空间/能力包/工具 + 能力包→客户端授权）；范围保存做 Catalog 引用校验；实时判定 |
| 5 | 执行 | tools/call 完全复用 execute 决策链；同步；默认已发布版本；需确认工具明确拒绝、不执行（D1 兜底） |
| 6 | 管理 | 独立"MCP 接入"一级菜单五个页面：接入配置、客户端与授权、暴露工具（只读）、调用记录、测试调试 |
| 7 | Trace | `runtime_trace_log` 增加 `channel` / `mcp_client_id` 维度，管理端开放 MCP 渠道查询 |
| 8 | D1 风险确认 | 延后待确认（记录于《待确认问题清单》《设计冻结》《待后续实现功能》）；开发完成后实现与调整 |
| 9 | D2/D3 技术细节 | SDK 鉴权接线与挂载/生命周期细节在阶段 0 技术预研中实证；如与冻结草案结论不一致，追加补充记录后再进入建模 |

## 3. 功能拆解

### 3.1 依赖与基础设施

- pyproject 引入 `mcp>=2,<3`；pydantic 升到 >=2.12，anyio、sse-starlette、opentelemetry-api、mcp-types、typing-extensions 对齐 SDK v2 下限；
- 显式保留 httpx 依赖（HTTP Provider 在用），审计 httpx/httpx2 异常分支；
- 技术预研 Spike：现有 FastAPI 上完成最小可运行 MCP 端点（initialize → tools/list → tools/call），验证挂载、lifespan、传输安全默认行为与 TokenVerifier 接线；
- 全量测试与 ruff 保持通过，作为依赖升级后的回归基线。

### 3.2 数据模型（可直接改基线，不做迁移）

- `catalog_tool` 增加 `http_enabled` / `mcp_enabled`（默认 true）；
- 新增 MCP Server 配置（全局启停、端点路径、来源/传输安全、协议版本范围）；
- 新增 MCP 客户端（编码、名称、状态、来源限制、Token 哈希、Token 重置/吊销状态）；
- 新增 MCP 客户端授权范围（命名空间 / 能力包 / 工具三级 + 能力包→客户端授权）；
- `runtime_trace_log` 增加 `channel` / `mcp_client_id` 及索引；
- 管理操作码增加 MCP 域操作码并纳入启动同步与角色分配。

### 3.3 认证链路

- Token 签发（明文仅显示一次）、哈希入库、重置/吊销、客户端启停；
- SDK 资源服务器校验接入：TokenVerifier 完成哈希比对 + 客户端状态 + Token 有效性 + 来源限制；
- 未认证/无效 Token 在协议解析前 401，不执行、不泄露工具信息。

### 3.4 授权与工具暴露

- 将 HTTP 调用系统范围判定抽象为"授权主体 × 授权范围"通用计算，HTTP/MCP 共用语义，避免口径漂移；
- MCP A2 授权解析：命名空间 / 能力包 / 工具 + 能力包→客户端授权，默认拒绝；
- tools/list 构建：客户端授权内 ∩ 工具启用 ∩ mcp_enabled ∩ 可发现 ∩ 已发布默认版本且 Provider 可用；
- 工具名直接映射 Catalog 完整编码，schema/描述复用 Catalog；
- HTTP 侧回归：调用控制接入 `http_enabled` 过滤，默认全开行为不变。

### 3.5 tools/call 执行闭环

- MCP 适配层解包 → 授权与实时状态重判 → 需确认工具拒绝（D1 兜底）→ JSON Schema 校验 → ProviderGateway → output_schema 校验 → 结果标准化为 MCP 响应；
- 错误映射：401、未授权/不可用、参数错误、Provider 错误、内部错误分别映射 MCP 错误语义；
- 同步执行，默认已发布版本；显式版本不进入本批契约；
- 事件落 Trace：认证、授权、执行、Provider、请求完成，带 channel 与 client_id。

### 3.6 管理端（MCP 一级菜单）

- 接入配置：全局启停、Server 信息、端点路径、来源/传输安全、协议版本范围、连接示例、运行摘要；
- 客户端与授权：客户端 CRUD、Token 签发/重置/吊销、来源限制、A2 授权配置；
- 暴露工具：只读展示 MCP 可发现集合，跳转共享 Catalog；
- 调用记录：MCP 渠道 Trace 列表与事件链详情；
- 测试调试：协议级调试台（见 3.7）；
- 前端菜单与按钮按新操作码控制，后端 API 独立鉴权。

### 3.7 协议级测试调试

- 专用内置测试客户端；以 SDK MCP 客户端对实际挂载端点发起 initialize → tools/list → tools/call；
- 输出协议层消息摘要、标准化结果、trace_id；Trace 标注 `source=mcp-console-test`；
- 高风险确认分支随 D1 延后；第一批验证"需确认工具被明确拒绝、不执行"；
- 只回环测试本平台 Server。

### 3.8 部署交付与验收

- MCP 运行入口网络边界：来源限制默认仅内网；对外域名需配置 Host/来源白名单；
- Nginx 路由/Header 清洗与配置示例、verify 脚本更新；
- 端到端验收：客户端创建 → Token 签发 → 授权 → tools/list → tools/call → Trace 查询；
- 兼容矩阵实测：Inspector + SDK 客户端必测；Cursor / Claude Desktop 抽测并记录；
- HTTP 既有闭环回归；文档对齐（需求/设计/开发完成情况/待后续功能）。

## 4. 可复用现状

| 资产 | 说明 |
|------|------|
| Catalog / 工具版本 / 审核发布 / 能力包 / Provider | 直接复用，不新建工具生命周期 |
| execute 决策链（调用控制、Schema 校验、ProviderGateway、结果标准化） | tools/call 直接复用 |
| TraceService / runtime_trace_log | 增加渠道维度后复用 |
| AuditService / management_audit_log | 管理操作接入 |
| 管理操作码体系与前端按钮/菜单控制 | 新增 MCP 域操作码后复用 |
| CallerToolScope 授权模型与判定逻辑 | 抽象为通用主体后为 MCP 复用 |
| 前端布局、测试基础设施、部署交付物 | 沿用 |

## 5. 工程约定

- 数据库结构变更直接修改 `sql/init.sql`，不保留历史迁移（无历史数据，可删库重建）；
- 状态约束使用代码层枚举，数据库不加 CHECK；
- 管理操作码集中声明于 `core/operation_codes.py`，启动同步；
- 关键管理操作接入 `AuditService`；敏感凭据只存哈希，不进入日志/Trace/审计；
- 后端只注入所需配置分区，不依赖完整 Settings；
- 测试与实现同步编写，每阶段跑通全量测试、ruff，前端改动跑通 tsc；
- SDK 版本固定 `mcp>=2,<3`，升级需全量回归并追加补充记录。

## 6. 实施顺序

| 阶段 | 内容 | 依赖 | 完成标志 |
|------|------|------|----------|
| 0 | 技术预研与依赖基线：SDK 挂载/鉴权 Spike、依赖升级、HTTP 回归 | 无 | 最小 MCP 端点跑通 initialize/tools/list/call；测试与 ruff 全绿；D2/D3 结论落档 |
| 1 | 数据模型与基础服务：协议开关、MCP Server/客户端/Token/授权表、Trace 渠道维度、操作码 | 阶段 0 | 表结构与枚举就绪；Token 签发/重置/吊销服务可用；引用校验生效 |
| 2 | 认证链路：TokenVerifier 接入、401 语义、来源限制 | 阶段 1 | 有效 Token 通过、无效/停用/吊销/来源不符被拒并留 Trace |
| 3 | 授权与暴露：主体抽象、A2 范围解析、tools/list、mcp_enabled、HTTP 回归 | 阶段 1、2 | 可发现集合正确；HTTP 既有行为回归通过 |
| 4 | tools/call 执行闭环：决策链复用、需确认拒绝、错误映射、Trace | 阶段 3 | 授权客户端可端到端同步调用低风险工具；需确认工具被拒绝且不执行 |
| 5 | 管理端：五个页面 + 管理 API + 操作码控制 | 阶段 1–4 | 管理员可在 MCP 菜单完成配置、客户端授权、调用记录查询 |
| 6 | 协议级调试台与兼容矩阵：内置测试客户端、Inspector/SDK 必测、Cursor/Claude 抽测 | 阶段 4、5 | 调试台可用；兼容矩阵落档，受限项登记待后续功能 |
| 7 | 部署交付与端到端验收：入口网络、verify、HTTP 回归、文档 | 全部 | MCP 接入完成判定全过 |

顺序说明：

- 阶段 0 必须先做：SDK v2 挂载/鉴权接线是后续模型与契约的前提，实证结论不一致时以补充记录修正设计冻结后再建模；
- 阶段 1 与阶段 2 可部分并行（认证依赖模型），建议按上表串行推进保证可验收；
- D1 风险确认不进入阶段 0–7：阶段 4 只落地"需确认工具拒绝"兜底；代码开发完成后作为后续迭代实现与调整，避免阻塞主线；
- 每阶段内部按"模型 → 服务 → API → 前端 → 测试 → 文档"推进。

## 7. 完成判定

以《需求》第 7 节验收标准为准：

1. 管理员可在 MCP 菜单完成 Server 配置、客户端创建与 Token 签发、客户端授权；变更实时生效。
2. 已发布且 mcp_enabled 的工具可被授权客户端 tools/list 发现，工具名等于 Catalog 完整编码，Schema 与 Catalog 一致。
3. 授权客户端可调用已发布默认版本，经 Schema 校验后正确返回结果；未授权/停用/关闭协议被拒且不泄露工具信息。
4. 需确认（高风险/写操作）工具在 MCP 渠道被明确拒绝、不执行（D1 兜底）。
5. MCP 调用全过程落 Trace；管理端"MCP 调用记录"可查询。
6. 工具全局停用、协议开关关闭、客户端停用/Token 吊销后，后续 MCP 调用立即被拒，在途请求自然完成。
7. Inspector + SDK 客户端实连必测通过；Cursor/Claude Desktop 抽测并记录兼容矩阵，受限项登记《待后续实现功能》。
8. HTTP 既有闭环不受影响；协议开关默认全开下 HTTP 行为保持。

## 8. 本批明确不做

- 方向 B/C（收编外部 MCP Server、双向模式）；
- MCP 风险确认流程（D1 延后：开发完成后实现与调整；第一批需确认工具拒绝、不执行）；
- OAuth2 / mTLS 客户端认证；
- 异步/流式执行、取消、回调、死信；
- resources / prompts 服务端能力；
- 显式工具版本经 MCP 调用（如需支持另行确认）；
- 通用 MCP Client 实验室；
- 管理端完整可观测看板/告警/审计报表。

## 9. 关联文档

- [待确认问题清单](待确认问题清单.md)
- [需求](需求.md)
- [设计冻结](设计冻结.md)
- [待后续实现功能](../../待后续实现功能/待后续实现功能.md)

## 10. 补充记录

> 本节仅追加补充记录，不修改上文内容。

### 2026-09-07

- 开发计划 v0.1 创建。D1（MCP 风险确认流程）按用户决定延后：先不处理、保持待确认，在 MCP 接入代码开发完成后实现与调整；本计划阶段 4 只落地"需确认工具拒绝、不执行"兜底。

## 11. 补充记录（阶段 0：技术预研与依赖基线完成）

> 本节仅追加补充记录，不修改上文内容。

### 2026-09-07

- 阶段 0 已按开发计划完成并通过验收：全量测试 443 项、ruff 检查全部通过。
- 依赖实证：mcp 2.1.1 安装成功，pydantic 2.13.4 / starlette 1.6.0 / anyio 4.14.2 等现有版本满足 SDK v2 下限，无解析冲突；sse-starlette 3.4.11、httpx2 2.12.0、mcp-types 2.1.1 随 SDK 引入；`tool-hive/pyproject.toml` 已新增 `mcp>=2,<3`、显式 httpx 依赖并将 pydantic 下限升至 >=2.12。
- `requires-python` 保持 `<3.12` 未放宽（本地 3.12 继续按项目既有 `--ignore-requires-python` 惯例安装）；SDK v2 要求 Python 3.10+，生产基线 3.11.6 满足。
- 挂载实证（D3）：`MCPServer` + `streamable_http_app(streamable_http_path="/")` 挂载到宿主 FastAPI 的 `/mcp` 前缀，完整公开端点为 `/mcp/`（带尾斜杠；不带尾斜杠时 SDK 返回 307 跳转）。宿主 lifespan 手动进入 `mcp.session_manager.run()` 已验证，进入后 initialize / tools/list / tools/call 全链路跑通。
- 鉴权接线实证（D2）：采用 SDK 原生 `TokenVerifier` + `AuthSettings`；无 Token 请求在协议解析前返回 401，有效 Token 可完成完整协议闭环。SDK 会发布 OAuth 资源元数据并返回标准 401 头，阶段 2 接入真实 Token 存储时复核该行为是否需要切换为宿主侧中间件。
- 新增占位工具 `ping`（位于 `toolhive/mcp/server.py`）与阶段 0 测试（`tests/mcp/test_mcp_stage0.py`）；占位工具将在阶段 3/4 替换为 Catalog 动态暴露，开发期临时 Token（`TOOLHIVE_MCP_DEV_TOKEN`）将在阶段 2 替换为真实客户端 Token 校验。

## 12. 补充记录（阶段 1：数据模型与基础服务完成）

> 本节仅追加补充记录，不修改上文内容。

### 2026-09-07

- 阶段 1 已按开发计划完成并通过验收：全量测试 450 项、ruff 检查全部通过（含新增 MCP 阶段 1 测试 7 项）。
- 数据模型落地：`catalog_tool` 增加 `http_enabled`/`mcp_enabled`（默认开启）；`runtime_trace_log` 增加 `channel`/`mcp_client_id`；新增 `mcp_server_config`（单实例配置，init.sql 插入 default 行）、`mcp_client`、`mcp_client_token`（仅存哈希，同一客户端仅一条 ACTIVE，部分唯一索引）、`mcp_client_ip_rule`、`mcp_client_scope`（client_id + scope_type + scope_code 唯一）。
- 确认项执行口径（按方案推荐）：
  - 能力包→MCP 客户端授权采用范围条目 `scope_type=capability` 表达，不新增独立能力包关联表；
  - MCP 客户端生命周期为 draft / enabled / disabled / revoked；启用前置条件为存在 ACTIVE 令牌与 ACTIVE 来源 IP 规则（镜像 HTTP 调用系统启用前置思路）。
- 操作码新增：`mcp_server:view/config`、`mcp_client:view/manage/allow_any_ip`、`mcp_auth:view/manage`，元数据随启动同步；`mcp_trace:view`/`mcp_test:run` 留待对应阶段新增。
- Catalog 范围引用校验已抽为共享服务（`services/catalog_scope_validator.py`），调用系统与 MCP 客户端共用同一实现；调用系统 `_validate_scope_references` 改为委托共享校验并全量回归通过。
- 管理 API（无前端）：`/api/admin/mcp/**` 提供 Server 配置、客户端 CRUD/启停/恢复/注销、Token 签发/吊销、来源 IP 规则、授权范围接口；工具创建/更新/响应支持协议开关字段。
- 待办接续：阶段 2（认证链路：以真实客户端 Token 校验替换开发期 TokenVerifier）、阶段 4（授权范围接入运行判定）、阶段 5（管理页面）仍按开发计划推进。

## 13. 补充记录（阶段 2：认证链路完成）

> 本节仅追加补充记录，不修改上文内容。

### 2026-09-07

- 阶段 2 已按开发计划完成并通过验收：全量测试 456 项、ruff 检查全部通过（新增 MCP 阶段 2 测试 6 项）。
- D2 定稿：改为宿主侧 MCP 鉴权中间件，SDK 不再承担 TokenVerifier/AuthSettings（原因与语义见《设计冻结》阶段 2 认证定稿补充记录）。
- 数据补充：`mcp_client_token` 新增唯一列 `token_key`（SHA-256 检索键），argon2 哈希仍用于最终比对；init.sql、ORM、签发服务与测试已同步。
- 阶段 0 开发期临时 Token（`TOOLHIVE_MCP_DEV_TOKEN`）与 SDK 原生 401 行为已移除；阶段 0 测试改为纯挂载/协议闭环测试。
- 认证服务与中间件：`toolhive/mcp/auth.py`（可测认证逻辑）+ `toolhive/mcp/middleware.py`（宿主侧拦截），已接入 `main.py`（入口中间件在外层先解析来源 IP）。
- 完成标志达成：有效 Token + 启用客户端 + 命中来源规则可进入 MCP 端点；无效/停用/吊销/来源不符/Server 停用均被拒并落 Trace。

## 14. 补充记录（阶段 3：授权与工具暴露完成）

> 本节仅追加补充记录，不修改上文内容。

### 2026-09-07

- 阶段 3 已按开发计划完成并通过验收：全量测试 461 项、ruff 检查全部通过（新增 MCP 阶段 3 测试 5 项）。
- Server 切换到官方 SDK v2 低层 `Server`，`on_list_tools` 按当前 MCP 客户端授权动态返回工具（技术结论见《设计冻结》阶段 3 工具暴露定稿）。
- 共享范围解析 `runtime/tool_control/scope_expansion.py` 已接入 HTTP 调用控制、运行中间件与 MCP 工具清单；HTTP 链路新增 `http_enabled` 过滤。
- MCP 可发现口径与默认已发布版本 Schema 组装已实现（`runtime/mcp_tools/service.py`）；tools/call（`on_call_tool`）属阶段 4。
- 阶段 0 协议测试收敛为 initialize/tools/list 往返，占位 ping 移除。

## 15. 补充记录（阶段 4：tools/call 执行闭环完成）

> 本节仅追加补充记录，不修改上文内容。

### 2026-09-07

- 阶段 4 已按开发计划完成并通过验收：全量测试 465 项、ruff 检查全部通过（新增 MCP 阶段 4 测试 4 项）。
- 新增 `runtime/mcp_tools/executor.py`：MCP `tools/call` 复用 Catalog execute 决策链——授权与实时状态重判 → 默认已发布版本 → 执行绑定/Provider 状态 → D1 兜底（需确认工具拒绝、不执行）→ Schema 校验 → ProviderGateway（builtin/http）→ output_schema 校验 → 结果标准化。
- `mcp/server.py` 注册 `on_call_tool`；结果映射为 MCP `CallToolResult`（文本 + structuredContent + isError），错误统一转 isError 语义，不泄露内部细节。
- Trace：runtime.control / runtime.provider / runtime.execute / runtime.request 事件落库，带 channel=mcp 与 mcp_client_id。
- 写操作绑定（POST/PUT/DELETE）与高风险工具本批统一按"需确认"拒绝，不进入 Provider；后续 D1 落地后调整。

## 16. 补充记录（阶段 5：管理页面完成）

> 本节仅追加补充记录，不修改上文内容。

### 2026-09-07

- 阶段 5 已按开发计划完成并通过验收：后端全量测试 469 项、ruff 全部通过，前端 `tsc` 构建检查通过。
- 操作码新增：`mcp_trace:view`（调用记录查询）、`mcp_test:run`（阶段 6 调试台权限位）。
- 后端管理 API 补充：`GET /api/admin/mcp/exposed-tools`（MCP 暴露工具只读查询）、`GET /api/admin/mcp/call-records`（MCP 渠道调用记录分页，按 trace 聚合摘要）与 `GET /api/admin/mcp/call-records/{trace_id}`（事件链详情）。
- 前端新增 "MCP 接入" 一级菜单与五个页面：接入配置、客户端与授权、暴露工具（只读）、调用记录、测试调试（占位页，阶段 6 提供协议级控制台）；页面按操作码控制菜单/按钮展示。
- 说明：暴露工具页仅展示当前 MCP 可发现集合；工具定义与协议开关编辑仍留在共享工具目录（单事实源）。

## 17. 补充记录（阶段 6：协议级调试台与兼容矩阵）

> 本节仅追加补充记录，不修改上文内容。

### 2026-09-07

- 阶段 6 已按开发计划完成并通过验收：后端全量测试 471 项、ruff 全部通过，前端 `tsc` 通过（新增 MCP 阶段 6 测试 2 项）。
- 新增 `toolhive/mcp/console.py`：固定内置调试客户端（`mcp-console-test`），每次调试自动保证客户端可用、配置来源规则、签发一次性 Token，会话结束后吊销；用官方 SDK 客户端对实际 MCP 挂载执行 initialize → tools/list → tools/call，返回协议摘要、结果与最近 Trace ID。
- 管理 API：`POST /api/admin/mcp/test-debug`（`mcp_test:run`）；前端测试调试页从占位升级为可运行表单（工具下拉、参数 JSON、结果展示）。
- 新增 [兼容矩阵](兼容矩阵.md)：SDK/参考客户端已实测通过；Inspector / Cursor / Claude Desktop 标注"待外部环境实测"，方法与回填规则见文档。
- 需确认（高风险/写操作）工具在调试台按 D1 兜底展示"拒绝、不执行"，符合当前交付口径。

## 18. 补充记录（阶段 7：部署交付与端到端验收）

> 本节仅追加补充记录，不修改上文内容。

### 2026-09-07

- 阶段 7 已按开发计划完成：后端全量测试 471 项、ruff 与前端 `tsc` 全部通过（MCP 功能批次 0–7 全部完成）。
- Nginx：`deploy/nginx/toolhive.conf` 新增 MCP 内网入口（默认 8082，仅转发 `/mcp/`，本机/内网来源限制与 Header 清洗）。
- `verify.sh`：新增 MCP 入口配置检查与可选 `TOOLHIVE_MCP_E2E=1`（Bearer + SDK 客户端 initialize → tools/list → tools/call）。
- `deploy/README.md`：新增 9.2「MCP 入口端到端验收」章节（客户端准备、Token、来源规则、授权范围、verify 命令与 Trace 核验）。
- 真实库 `init.sql` 冒烟：当前环境无 psql/数据库连接，**未执行**；已在 deploy README 与本节记录待部署环境执行的命令与预期（新表/列、索引、默认 Server 配置行）。
- 兼容矩阵：SDK/参考客户端实测通过；Inspector / Cursor / Claude Desktop 标注待外部环境实测（见 `兼容矩阵.md`）。
- 收口说明：MCP 接入第一批（0–7 阶段）开发完成；D1 风险确认流程、异步/流式执行等按既有登记延后；真实库/真实客户端实测与部署验收待部署环境执行后回填。
