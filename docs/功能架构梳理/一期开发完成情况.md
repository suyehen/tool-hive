# ToolHive 一期开发完成情况

> 更新日期：2026-08-16  
> 文档目的：集中说明一期上半部分已经完成开发的内容，以及一期下半部分尚未完成的内容。  
> 范围说明：本文件不展开已开发功能的缺陷、整改、测试结论或验收问题；相关事项由独立问题文档维护。
>
> 架构边界：管理模块与工具应用模块（运行 API）属于同一个 ToolHive 应用，统一部署、启动、停止和升级。本文的“上半部分”和“下半部分”仅表示一期交付顺序，不表示两套可独立运行的后端系统。

## 1. 当前阶段结论

当前项目处于“一期上半部分管理模块优先完成开发，一期下半部分工具应用模块尚未完成”的阶段。

这里的“已完成开发”表示代码库中已经具备对应后端或前端实现，不等同于问题整改、生产部署或最终验收已经全部完成。本文仅描述功能开发范围和当前代码状态。

## 2. 一期上半部分：管理模块已完成开发内容

一期上半部分已经完成开发的范围集中在管理模块基础能力，包括应用基础、管理身份、权限、调用系统配置和管理前端。

| 模块 | 已完成开发内容 |
|------|----------------|
| 应用基础 | 已建立 Python/FastAPI 应用入口、配置加载、PostgreSQL 异步会话、Redis 客户端、统一异常基础和健康检查。 |
| 管理 API 容器 | 已建立同一 ToolHive 应用内的 `/api/admin/**` 管理 API 模块，接入会话中间件、CSRF 中间件，并注册认证、账号、角色、调用系统等路由；管理 API 已统一接入后端操作级鉴权（`require_operation`，未授权返回 403）。 |
| 管理账号 | 已建立管理账号数据模型和管理 API，支持账号列表、创建、启用、禁用、解锁、密码重置和强制下线；管理前端已提供对应页面。 |
| 本地登录与会话 | 已建立用户名密码登录、MFA 绑定与验证、恢复码登录、密码修改、登出、会话查询和 CSRF Token 接口。 |
| 安全基础服务 | 已实现密码哈希与强度校验、临时密码生成、TOTP 密钥加密、恢复码、会话服务、登录失败限制和 CSRF Token 服务。 |
| 后台角色 | 已建立后台角色、账号角色关联、角色操作项关联等数据模型，以及角色创建、修改、状态管理、操作项查询和分配 API。 |
| 管理操作项 | 已建立管理操作码枚举、管理操作项数据模型和操作项查询/分配能力，为后续页面与 API 操作级控制提供基础。 |
| 调用系统 | 已建立调用系统、调用系统公钥、来源 IP 规则的数据模型和管理 API，支持登记、生命周期操作、公钥管理和 IP 规则管理。 |
| 管理前端 | 已建立 React 管理端，包含登录页、MFA 设置页、首页、管理账号页、后台角色页和调用系统页。 |

### 2.1 已完成的管理模块功能关系

```text
管理前端
  ├── 登录 / MFA / 会话
  ├── 管理账号
  ├── 后台角色与管理操作项
  └── 调用系统、公钥与来源 IP 规则
            │
            ▼
        /api/admin/**
            │
            ▼
PostgreSQL（管理配置） + Redis（会话与安全辅助状态）
```

### 2.2 一期上半部分的范围边界

能力包、Provider、工具、工具版本、执行绑定、审核、发布、启停和归档已确认归入一期下半实现，不作为一期上半部分的目标或完成判定；其核心模型和状态规则需在实现前完成确认，当前尚未实现，作为一期下半工具应用链路的配置前提。

## 3. 一期下半部分：未完成内容

一期下半部分的目标是在同一个 ToolHive 应用内，把调用系统配置、工具目录和安全规则接入工具应用 API，形成受控 HTTP 工具调用闭环。当前以下内容尚未完成。

| 模块 | 未完成内容 |
|------|------------|
| 工具应用模块（运行 API） | 尚未建立并挂载 `/api/runtime/v1/**` 的路由、服务和统一请求处理链；该模块将与管理模块在同一 ToolHive 应用中统一启停。 |
| 工具应用入口与网关交付 | 尚未形成工具应用 API 的内网入口、Nginx 路由、安全 Header、代理来源处理和生产部署的完整交付物。 |
| 调用系统认证校验 | 尚未形成 RSA-PSS 请求签名验证、请求体摘要校验、时间窗口校验、Nonce 共享存储和认证中间件。 |
| 工具应用 API 范围与流量控制 | 尚未实现按调用系统限制 API、工具范围、QPS、并发、超时、配额、熔断和紧急禁用的运行机制。 |
| ToolContext | 尚未落地调用系统传递用户、租户、角色、渠道、会话和追踪信息的统一契约与可信校验。 |
| 工具调用控制 | 尚未实现调用系统、业务身份、能力包、工具范围和策略之间的权限计算。 |
| Catalog 核心配置 | 尚未实现能力包、Provider、工具、工具版本、执行绑定、审核、发布、启停等模型、管理 API 和管理页面。 |
| 工具发现 | 尚未实现 Resolve/Discover、候选工具筛选、工具 Schema 下发、覆盖度判断和无权限信息控制。 |
| 工具执行 | 尚未实现 Execute、高风险确认、JSON Schema 校验、参数级安全校验、结果标准化和错误处理。 |
| HTTP Provider | 尚未实现固定 HTTP 请求映射、目标地址白名单、SSRF 防护、超时、重试、幂等、响应限制与外部 API 接入；一期不包含目标系统凭据代持。 |
| 工具检索 | 尚未实现 Catalog 到 Chroma 的索引、Embedding、关键词/向量召回、排序和检索不可用降级。 |
| Trace 与审计 | 整体不属于一期上半完成内容；基础 Trace/审计统一纳入一期下半设计与实现，届时回补管理侧审计、脱敏和留存规则。 |
| 基础设施交付 | 尚未形成数据库迁移、初始化命令、生产配置、备份恢复、容量和可用性验收的完整交付。 |

### 3.1 一期下半部分工具应用调用链

```text
调用系统
  → Nginx 内网来源限制
  → 调用系统状态、IP 白名单和公钥签名校验
  → Nonce 防重放
  → 运行 API 范围与工具调用控制
  → Discover 或 Execute
  → 已发布工具定义 + HTTP Provider 受控调用
  → 结果标准化、Trace 与审计
```

上述链路尚未在当前代码中形成可运行闭环。

### 3.2 一期下半部分 Provider 出站与执行安全待开发项

> **归属说明**：本节属于一期下半部分工具应用模块的待开发和待确认内容，不属于一期上半部分管理模块的完成判定。上半部分对外查询能力的具体范围另行确认。

#### 已确认边界

- 一期调用系统认证只保存调用系统公钥；调用系统私钥由调用系统自行保管。
- 一期不保存目标业务系统的密码、API Key、OAuth Secret 或客户端私钥。
- 一期不引入 Secret Store、`credential_ref` 或目标系统凭据代持接口。
- 如果首批工具需要目标系统凭据，该工具不得进入一期交付，转入二期重新确认。
- ToolHive 只能通过已审核的固定 Provider 映射访问外部接口，不能把调用方或 LLM 传入的 URL、方法、认证 Header 当作执行目标。
- Provider 执行必须经过统一的 `ProviderGateway`，管理 Controller、运行 Controller 和其他业务模块不得自行访问任意外部地址。
- 一期不执行任意 Python、Shell、文件系统操作或动态代码；高风险宿主能力转入后续独立执行器评估。

#### 待确认和待实现的出站安全规则

- 固定 Provider 目标的域名、端口、路径模板、HTTP 方法、参数映射和允许 Header。
- DNS 解析后对全部 IPv4/IPv6 地址进行校验，拒绝回环、链路本地、云元数据、多播和未授权私网地址；确需访问内部系统时，必须显式配置允许的 CIDR。
- 一期默认禁止自动跟随 HTTP 重定向；如后续允许，每一跳都必须重新校验目标和 TLS。
- 强制 HTTPS 和证书校验，不允许关闭 TLS 验证或使用调用方指定的代理。
- 应用层目标校验与生产主机防火墙形成双层出站限制；动态地址 Provider 是否通过独立 Egress Proxy，需要根据实际 Provider 再确认。
- 限制请求体、响应体、Header 数量、连接时间、读取时间、总执行时间和并发数。
- 读操作与写操作采用不同的超时、幂等、重试和熔断规则；写操作在外部状态未知时不得盲目自动重试。
- Provider 返回结果必须经过格式校验、大小限制、标准化、脱敏和 Trace 记录，不能把返回内容直接当作平台指令或新的工具定义。
- Provider 出站失败、目标校验失败、TLS 失败、响应超限和熔断状态需要形成可查询的运行日志和审计信息。

#### 与一期上半部分的关系

- 上半部分只为下半部分提供调用系统、公钥、工具目录、审核发布和安全规则等配置前提。
- 下半部分负责将这些已发布配置接入运行 API，并完成 Discover、Execute 和 Provider 受控调用。
- 上半部分对外查询能力的具体对象、接口和范围尚未冻结，不在本节中预设。

### 3.3 Trace 与审计阶段安排

Trace 与审计整体放在一期下半统一设计，不作为一期上半管理模块的完成判定。这样可以围绕真实的工具发现、工具执行和 Provider 调用链路确定追踪边界，再回头补齐管理侧操作的关联记录。

一期下半至少需要确认和实现：

- 运行请求的 `trace_id` 生成、传递和错误关联；
- 调用系统认证、授权、Discover、Execute、Provider 调用和结果处理的关键记录；
- 管理变更和运行决策的基础审计、脱敏和留存规则；
- Trace 与审计记录的查询关联方式。

一期上半现有的登录、权限和管理配置功能不因暂未实现 Trace/审计而改变；相关能力在一期下半开始前统一回补和验收。生产级健康检查、metrics、监控告警、备份恢复、RPO/RTO、发布迁移和多实例高可用不属于本节，延后至二期。

## 4. 上下半部分衔接依赖

一期下半部分不能只从工具应用 API 开始开发，需要先完成以下跨阶段前置项：

| 前置项 | 与下半部分的关系 |
|--------|------------------|
| Catalog 核心模型 | 工具应用模块必须知道 Provider、工具、版本、固定执行配置和目标边界，才能发现或执行工具；一期不包含目标系统凭据引用。 |
| 审核、发布与版本规则 | 工具应用模块必须能够判断哪些工具版本允许被发现和执行。 |
| 调用系统启用范围 | 工具应用模块必须知道调用系统可访问哪些 API、能力或工具。 |
| ToolContext 契约 | 工具调用控制必须基于可信的用户、租户、角色和渠道上下文进行判断。 |
| 工具调用控制模型 | Discover 与 Execute 都依赖明确的能力、策略、范围和高风险确认规则。 |
| 首批外部 HTTP API 契约 | Provider 执行需要固定的请求/响应、业务权限、幂等、超时和重试约定。 |
| Trace 与审计要求 | 归入一期下半统一设计；工具运行链路先确定基础 `trace_id` 和审计字段，再回补管理侧关联和留存规则。 |

## 5. 当前代码结构快照

当前实际代码主要集中在一期上半部分：

```text
tool-hive/
  src/toolhive/
    main.py                    当前 FastAPI 入口，仅挂载 /api/admin
    api/admin/
      auth/                    登录、MFA、会话、CSRF
      accounts/                管理账号
      roles/                   后台角色与操作项
      caller_systems/          调用系统、公钥、IP 规则
    models/                    管理账号、角色、调用系统等 ORM 模型
    services/                  账号、认证、角色、调用系统与安全服务
    infrastructure/            PostgreSQL 与 Redis 连接

frontend/src/
  pages/
    LoginPage                  登录
    MfaSetupPage               MFA 设置
    accounts/                  管理账号页面
    roles/                     后台角色页面
    caller-systems/            调用系统页面
```

当前没有工具应用模块的 `api/runtime`，也没有 Provider、Tool、ToolVersion、Capability、Trace 等领域模型实现。

## 6. 当前状态摘要

> 一期上半部分中已确认的管理基础功能已经完成开发；一期下半部分的工具应用 API、工具 Catalog、工具调用控制、HTTP Provider、检索和 Trace 闭环尚未完成。一期最终目标仍在继续实施。

---

## 7. 补充记录

> 本节仅追加补充记录，不修改上文内容。

### 2026-08-20（H03）：首个超管初始化与最后超管保护

- 管理账号模块已补齐 `toolhive init-admin` CLI 初始化入口；密码来源为 `TOOLHIVE_INIT_ADMIN_PASSWORD` 环境变量或交互输入，用法已写入 CLI `--help` 与 `toolhive.example.yaml`。
- `init_super_admin` 已确保账号自动关联内置超管角色；`_check_last_super_admin` 已实现最后启用超管保护（禁用/离职最后一个启用超管被拒绝）。
- 新增 `GET /api/admin/bootstrap/status` 初始化状态查询接口。
- 初始化审计记录待 H07 确认管理审计口径后统一补充。

### 2026-08-20（H04）：传统图形验证码登录链路

- 新增图形验证码生成与一次性校验：`POST /api/admin/auth/captcha/challenge` 返回验证码标识与 PNG 图片，答案存 Redis（默认 300 秒有效）；无论校验正确或错误，挑战均被消费，不可重放。
- 登录请求新增必填字段 `captcha_id`/`captcha_code`，图形验证码先于账号流程校验；验证码错误/过期返回 401 并提示刷新。
- 移除验证码阈值触发的半成品逻辑（`captcha_required`、`check_captcha_required`）；配置项 `login_failure_window_minutes`（原 `captcha_trigger_window_minutes` 更名）、`captcha_ttl_seconds`、`captcha_code_length` 已同步写入示例配置。
- 前端登录页支持验证码展示、点击刷新与提交；MFA 相关步骤与页面保留至 H05 移除。

### 2026-08-20（H05）：移除 MFA，管理登录改为传统账密 + 图形验证码

- 登录接口在图形验证码与账号密码校验通过后直接创建管理 Session，返回 `session_id`/`csrf_token`；删除 MFA 验证、恢复码登录、MFA 绑定三个接口及对应前端页面（`MfaSetupPage`）、路由与 `qrcode.react` 依赖。
- 清理 `MfaConfig` 模型、`totp` 服务、TOTP 常量与 `totp_encryption_key` 配置；`sql/init.sql` 移除 `mfa_config` 建表语句（已确认环境无 MFA 数据，历史库如需清理可执行幂等 `DROP TABLE IF EXISTS mfa_config;`）。
- 删除 TOTP 相关测试，新增“验证码 + 密码正确直接登录成功”用例；全量测试 102 项通过。

### 2026-08-20（H06）：调用系统运行策略与工具范围管理接口

- 新增 `caller_runtime_policy`（运行 API 范围、QPS/并发/配额/超时/熔断、有效期）与 `caller_tool_scope`（工具/能力包范围）两张表；`caller_system` 增加系统级紧急禁用字段，`sql/init.sql` 同步建表与幂等迁移。
- 新增 `runtime-policy`（查询/保存）、`tool-scopes`（查询/全量替换）、`emergency-disable`/`emergency-enable` 六个管理接口，统一要求 `caller_system:policy` 操作码；流量策略并入 `runtime-policy`，不单独提供 `traffic-policy` 接口。
- 调用系统启用校验新增“运行策略已配置且 API 范围非空”检查；工具/能力包编码的引用校验待一期下半 Catalog 落地后补充。
- 全量测试 113 项通过。

### 2026-08-20（H07）：管理审计口径确认

- 正式口径：管理变更基础审计属一期下半必交付（追加式、脱敏、可关联 `trace_id`、覆盖关键安全事件、成功变更与审计同事务提交）；完整管理操作审计（查询、导出、报表、合规审计、留存策略）属二期。
- 一期目标完成判定第 6 条继续生效；一期开发完成情况“Trace 与审计整体放一期下半”表述与基础审计属一期下半一致。
- H03 初始化审计暂以结构化 INFO 日志替代，一期下半按本口径补正式审计记录。

### 2026-08-20（H07 实现）：基础管理审计落地

- 新增 `management_audit_log` 表与 `AuditService`：成功审计与业务同事务提交，失败审计（初始化失败、最后超管保护拒绝）独立事务立即提交；摘要敏感字段自动脱敏，预留 `trace_id`。
- 账号、角色、调用系统（含公钥、IP 规则、运行策略、工具范围、紧急禁用）关键管理操作已接入审计埋点，操作人由 `require_operation` 注入。
- 初始化超管审计已由 INFO 日志升级为正式审计记录；全量测试 121 项通过。

### 2026-08-20（H08）：状态约束、时间格式与凭据范围文档整改

- 状态约束口径统一为代码层枚举（`core/enums.py`，数据库不加 CHECK），A13 中“应用枚举 + PostgreSQL CHECK”旧方案已标注废弃。
- 接口时间统一为 ISO 8601 UTC（`2026-08-18T08:30:00Z`）；`time_utils` 输出格式已调整，模型中 3 处无时区 `datetime.utcnow()` 改为带时区 `datetime.now(UTC)`；数据库时间字段均为 TIMESTAMPTZ。
- 总体架构 Provider 段“凭据引用”标注为二期能力；A11“待填写”由延后结论覆盖。

### 2026-08-20（H09）：A01 运行 API 旧路径文档修正

- A01 中残留的 `/api/runtime/**` 旧写法已标注废弃，统一为 `/api/runtime/v1/**`；当前无运行 API 路由，无代码修改。

### 2026-08-20（H10）：管理登录来源 IP 统一使用可信入口解析结果

- 认证路由新增 `_get_client_ip`，登录与改密来源 IP 改为读取入口中间件写入的 `request.state.client_ip`，不再直接读取 `X-Forwarded-For` 等代理 Header。

### 2026-08-20（H11）：一期迁移、初始化、网关与基础验收交付物

- 新增轻量 SQL 迁移流程（`sql/migrations/` + `toolhive db-migrate` + `schema_migrations` 表）与 Nginx 配置示例（管理/运行入口、Header 清洗、可信代理）。
- 新增生产配置校验（`debug=false` 下密钥、连接串、回环绑定与可信代理强制校验，不满足拒绝启动）与部署脚本（install/start/verify）。
- 新增 `deploy/README.md` 部署与验收文档（环境、安装、迁移、初始化、Nginx、启动、验收、失败恢复与配置缺失说明）。

### 2026-08-20（H12）：管理前端与会话响应字段同步

- 前端删除登录/会话类型中的 `is_super_admin`（角色对象的该字段保留）；登录成功与刷新会话统一加载 `/auth/me` 与 `/auth/me/operation-items`。
- 认证上下文新增账号资料与实时操作项；侧边菜单按操作项过滤，后端操作码校验保持独立兜底；Dashboard 改为显示账号状态与操作项数量。
- 前端 `tsc` 类型检查通过。

### 2026-08-20（H12 补充）：账号离职接口与前端收尾

- 新增 `POST /api/admin/accounts/{account_id}/offboard` 账号离职接口（含最后超管保护与审计），前端账号页新增“离职”操作。
- 新增 `/change-password` 修改密码页面与路由，改密后重新拉取会话/CSRF。
- 账号、角色、调用系统页面的按钮按操作项显示/隐藏；H清单问题总览表追加整体状态说明。

### 2026-08-21（审计字段收敛与增改写入）

- 业务表审计字段由 `create_id` / `update_id` / `create_name` / `update_name` 收敛为 `create_by` / `update_by`（仅存操作人 ID，不再存名称）；`AuditMixin` 与 `sql/init.sql` 已同步。
- 按当前阶段约定直接修改 `sql/init.sql`（表结构变更不保留历史迁移记录），新建库执行 `init.sql` 即为新结构。
- 账号、角色、调用系统（含公钥、IP 规则、运行策略、工具范围）的创建/修改/状态变更路径显式写入 `create_time` + `create_by` / `update_time` + `update_by`，操作人取自 `require_operation` 注入的当前账号 ID；CLI 初始化与系统内部记录不写操作人。
- 补充审计字段写入断言用例；全量测试 129 项通过，ruff 检查通过。
